version: '3.8'

services:
  app:
    build: .
    ports:
      - "${APP_PORT:-8094}:8094"
    volumes:
      - .:/app
    environment:
      NODE_ENV: development
      WS_HOST: localhost
      WS_PORT: 8080
    command: npm start